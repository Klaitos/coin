<%- contentFor('body') %>
<h2>Coins list</h2>
  <ul>
    <% coins.forEach(function(coin) { %>
      <li>
        <strong>[<%= coin._id %>]</strong> <%= coin.name %>
      </li>
      <% }); %>
  </ul>